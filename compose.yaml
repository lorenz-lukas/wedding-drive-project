services:
  app:
    build:
      context: .
    ports:
      - "30000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: development
      PORT: 3000
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
